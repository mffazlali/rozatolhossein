import { LinkContent } from './components';

interface LinkFeatureProps {
  locale?: string;
}

const LinkFeature = ({ locale }: LinkFeatureProps) => {
  return <LinkContent />;
};

export default LinkFeature;
